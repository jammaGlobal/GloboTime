@page
@model timeZoneApp.Pages.Regions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Region[0].RegionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Region[0].offset)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Region[0].offsetType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Region[0].mostRecentDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Region) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RegionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.offset)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.offsetType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mostRecentDateTime)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.RegionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.RegionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.RegionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
